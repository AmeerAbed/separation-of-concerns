// do you find yourself doing this?
const coach1 = 'luis';
const coach2 = 'mateus';
const coach3 = 'gregory';

// maybe it's time for this
const arrayOfCoaches = ['luis', 'mateus', 'gregory'];

// you can access individual entries like this:
// (confusingly, the first entry is actually 0!)
console.assert(arrayOfCoaches[0] === 'luis', '0 is luis');
console.assert(arrayOfCoaches[1] === 'mateus', '1 is mateus');
console.assert(arrayOfCoaches[2] === 'gregory', '2 is gregory');
